---
- name: "upgrade packages."
  become: true
  apt:
    upgrade: "yes"

- name: "install dependencies."
  shell: |
    apk add --update curl nodejs npm

- name: "install pm2"
  become: true
  npm:
    name: pm2
    global: yes
    production: yes
    state: present